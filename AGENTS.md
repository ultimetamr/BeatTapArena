# Beat Tap Arena runtime

- Keep every source file, test, build output, screenshot, and APK inside `BeatTapArena`.
- Use PICO SpatialUI components under `PicoTheme`; never import Material or Material3.
- The app is a Mixed Stage with one bounded front-facing panel and no camera movement.
- Gaze + pinch and controller activation must remain equivalent for every primary action.

