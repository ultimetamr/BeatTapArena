<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".platform.SpatialApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeatTapArena">
        <activity
            android:name=".platform.LaunchActivity"
            android:exported="true"
            android:theme="@style/Theme.BeatTapArena">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="pico.spatial.stage.id" android:value="BeatTapArenaStage" />
            <meta-data android:name="pico.spatial.stage.style" android:value="1" />
            <meta-data android:name="pico.spatial.stage.immersion" android:value="100" />
            <meta-data android:name="pico.spatial.stage.immersion_min" android:value="100" />
            <meta-data android:name="pico.spatial.stage.immersion_max" android:value="100" />
        </activity>
    </application>
</manifest>

